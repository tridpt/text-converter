# Host interface to bind (use 0.0.0.0 to expose on the network / in Docker)
HOST=127.0.0.1

# Port the web server listens on
PORT=8000

# Maximum accepted upload size per request, in megabytes
MAX_UPLOAD_MB=25
